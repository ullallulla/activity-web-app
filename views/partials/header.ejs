<!DOCTYPE html>
<html>
    <head>
        <title>WSD Project</title>
        <meta charset="utf-8">
        <script src="/static/code.js" defer></script>
        <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.5.3/dist/css/bootstrap.min.css">
        <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">

    </head>
    <body>
    <div>
        <nav class="navbar navbar-expand-lg navbar-light bg-light justify-content-end">

            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav mr-auto">
                    <li class="nav-item">
                        <a class="btn btn-outline-primary m-1" href="/">Home <span class="sr-only">(current)</span></a>
                    </li>
                    <% if (user) { %>
                        <li class="nav-item">
                            <a class="nav-link m-1" href="/behavior/reporting">Reporting</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link m-1" href="/behavior/summary">Summary</a>
                        </li>
                    <% } %>
                </ul>
                <% if (!user ) { %>
                <ul class="navbar-nav">
                    <li class="nav-item">
                        <a class="btn btn-primary m-1" href="/auth/login">Login</a>
                    </li>
                    <li class="nav-item">
                        <a class="btn btn-outline-secondary m-1" href="/auth/register">Register</a>
                    </li>
                <% } %>
                    
                    <% if (user) { %>
                        <span class="navbar-text m-1">
                            <%= user.email %>
                        </span>

                        <form method="POST" action="/auth/logout"  >
                            <button class="btn btn-danger m-1">Logout</button>
                        </form>

                    
                    <% } %>

                </ul>
            </div>
        </nav>